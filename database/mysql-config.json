{
  "mysql": {
    "host": "localhost",
    "port": 3306,
    "database": "diffagent_db",
    "username": "diffagent_user",
    "password": "${MYSQL_PASSWORD}",
    "connectionLimit": 10,
    "acquireTimeout": 60000,
    "timeout": 60000,
    "charset": "utf8mb4",
    "timezone": "+00:00",
    "ssl": false,
    "pool": {
      "min": 2,
      "max": 20,
      "idleTimeoutMillis": 30000,
      "createTimeoutMillis": 30000,
      "acquireTimeoutMillis": 60000
    }
  }
}